apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-warehouses-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  hostPath:
    path: /mnt/data/mongo-warehouses
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-warehouses-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""
  volumeName: mongo-warehouses-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-warehouses-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-warehouses
  template:
    metadata:
      labels:
        app: mongo-warehouses
    spec:
      containers:
        - name: mongo
          image: mongo:6.0
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-warehouses-username
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-warehouses-password
            - name: ADMIN_WAREHOUSES_PASSWORD 
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-warehouses-password
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
          readinessProbe:
            tcpSocket:
              port: 27017
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 27017
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: mongo-storage
          emptyDir: {}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-warehouses-init
data:
  init-mongo.sh: |
    #!/bin/bash
    
    until mongosh --host 127.0.0.1 --eval 'quit(db.runCommand({ ping: 1 }).ok ? 0 : 2)' &>/dev/null; do
      echo "Aguardando o MongoDB..."
      sleep 1
    done

    mongosh --host 127.0.0.1 <<EOF
      use admin
      db.createUser(
        {
          user: "admin",
          pwd: "$ADMIN_WAREHOUSES_PASSWORD",
          roles: [ { role: "userAdminAnyDatabase", db: "admin" } ]
        }
      )
      db.auth("admin", "$ADMIN_WAREHOUSES_PASSWORD")
    EOF
---
apiVersion: v1
kind: Service
metadata:
  name: warehouses-mongo
spec:
  selector:
    app: mongo-warehouses
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
